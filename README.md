# Identifying-Root-Causes-of-Global-Temperature
Objective: Build a regression analytical pipeline to identify the root causes of global temperature change. Collect data from multiple scientific data sources, merge them into a single dataset, train regression models, and use feature importance to determine which environmental factors contribute most to temperature variation.
